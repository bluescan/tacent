<def f='include/c++/11/bits/postypes.h' l='183' ll='189' type='fpos&lt;_StateT&gt; std::fpos::operator+(std::streamoff __off) const'/>
<doc f='include/c++/11/bits/postypes.h' l='177'>// The standard requires that this operator must be defined, but
      // defines its semantics only in terms of operator-. In this
      // implementation it constructs a copy of *this, adds the
      // argument to that copy using operator+= and then returns the
      // copy.
      /// Add position and offset.</doc>
